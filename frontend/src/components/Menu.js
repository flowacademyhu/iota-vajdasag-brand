import React from "react";
import { NavLink } from "react-router-dom";

const items = ["Felhasználók", "Termékek", "Események", "Kijelentkezés"];

export default function Menu() {
    return (


        
        );
    }